#!/bin/bash
# MacOS startup script generated by JavaPackager plugin
SCRIPT_PATH=`cd "$(dirname "$0")" ; pwd`
osascript -e "do shell script quoted form of \"$SCRIPT_PATH\" & \"/${info.launcherScript} $@\" with administrator privileges" &