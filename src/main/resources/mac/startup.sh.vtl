#!/bin/sh
# MacOSX startup script generated by JavaPackager Maven Plugin
SCRIPTPATH="$(dirname "$(readlink "$0")")"
JAVA=$SCRIPTPATH/jre/bin/java
BINARY=$SCRIPTPATH/${app.name}
#if($app.isAdministratorRequired())
security execute-with-privileges $JAVA -jar $BINARY $@
#else
$JAVA -jar $BINARY $@
#end
exit 0
